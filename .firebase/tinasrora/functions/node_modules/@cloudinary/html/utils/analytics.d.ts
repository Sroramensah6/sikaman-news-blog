import { AnalyticsOptions } from "../types";
export declare const getAnalyticsOptions: (options?: AnalyticsOptions) => {
    trackedAnalytics: {
        sdkCode: string;
        sdkSemver: string;
        techVersion: string;
    };
};
